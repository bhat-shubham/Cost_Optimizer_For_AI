# ── AI Cost Optimizer — Environment Variables ──────────────
# Copy this file to `.env` and fill in your values.
# NEVER commit the real .env to version control.

# Required: async PostgreSQL connection string
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/ai_cost_optimizer

# Optional
APP_NAME=AI Cost Optimizer
DEBUG=true
ENVIRONMENT=dev
